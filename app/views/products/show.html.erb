<%= @product.description %>
<% @product.images.each do |image| %>
  <%= image_tag image.variant(:large)  %>
<% end %>

<%= link_to 'Root', root_path, class: 'btn btn-success' %>
<%= link_to 'Destroy', product_path(@product), data: { 'turbo-method': :delete } %>
